# AGENTS.md

本文件用于指导在本仓库中工作的编码 Agent。目标是以最小、可验证的改动维护 `memos`，不要在当前任务之外扩展范围。

## 项目概览

`memos` 是一个 Rust 2021 编写的本地备忘录工具，同时提供交互式 TUI 和只读 CLI 列表模式。

- `src/main.rs`：程序入口；解析参数、打开数据库并选择 CLI 或 TUI 模式。
- `src/cli.rs`：`clap` 子命令、机器可读输出、stdin 处理及稳定 CLI 错误码。
- `src/db.rs`：SQLite 初始化、兼容迁移和 CRUD；默认数据库位于系统数据目录。
- `src/model.rs`：共享领域模型 `Memo`。
- `src/tui/app.rs`：TUI 状态、筛选、编辑、选择、滚动及数据库操作。
- `src/tui/event.rs`：键盘和鼠标事件到 `App` 方法的映射。
- `src/tui/ui.rs`：`ratatui` 布局与渲染。
- `src/tui/mod.rs`：终端初始化、事件循环和终端状态恢复。

主要依赖为 `ratatui`、`crossterm`、`rusqlite`、`clap`、`chrono`、`directories`、`anyhow` 和 `unicode-width`。

## 铁律

以下规则优先级最高。每次完成代码修改后都必须执行，不得因为改动较小而跳过：

1. 运行与改动相关的测试，并尽可能运行完整的 `cargo test`；测试失败时不得提交或推送。
2. 使用 `cargo install --path . --force` 将当前版本安装到本地，确认安装成功。
3. 只暂存本次任务涉及的文件，不得夹带、覆盖或撤销用户已有改动。
4. 创建内容明确的 Git commit，不得把已完成的代码修改留在未提交状态。
5. 将该 commit 推送到当前分支对应的远程仓库；推送失败时应排查并重试，无法解决则明确报告原因。

提交前必须检查 `git status` 和待提交 diff，推送后必须确认本地分支与远程分支同步。除非用户明确要求，否则不要使用 `--force` 或 `--force-with-lease` 推送。

## 常用命令

优先使用 Cargo，不要引入额外任务运行器。

```bash
cargo check
cargo test
cargo run
cargo run -- --list
cargo build --release
```

修改 Rust 源码后，对涉及的文件执行格式化，并确保没有顺带格式化无关文件：

```bash
rustfmt --edition 2021 path/to/changed.rs
```

需要检查整个仓库时可运行：

```bash
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
```

注意：仓库当前可能存在任务开始前已有的格式差异。不要仅为让全仓库格式检查通过而修改与任务无关的代码；应在交付说明中如实指出基线问题。

## 修改原则

- 先阅读相关模块和已有测试，再修改代码。
- 保持改动最小，不做无关重构、重命名、依赖升级或视觉风格调整。
- 遵循现有 Rust 风格：使用 `anyhow::Result` 传播应用层错误，数据库参数使用 `rusqlite::params!`，不要随意使用 `unwrap()` 或 `expect()`。
- 用户可见文本当前以中文为主；新增提示、错误和快捷键说明应保持一致。
- 不要直接编辑 `Cargo.lock`。只有依赖确实发生变化时，才让 Cargo 更新它。
- 不要提交 `target/`、本地数据库、终端截图或其他生成文件。

## 数据库约束

- 默认数据库是真实用户数据。开发和自动化测试不得读写默认数据库路径。
- 测试数据库逻辑时使用 `Database::open_in_memory()`；需要验证迁移时使用隔离的临时路径。
- Schema 变更必须兼容已有数据库，迁移需要可重复执行，并补充旧 Schema 升级测试。
- SQL 必须参数化，不要通过字符串拼接写入用户输入。
- 修改查询时保留当前排序和归档语义，除非任务明确要求改变。

## TUI 约束

- `App` 负责状态和行为，`event.rs` 负责输入映射，`ui.rs` 负责渲染；不要把数据库写入或复杂状态变更塞进 UI 绘制函数。
- 保持键盘和鼠标操作一致，并留意 `Normal`、`Editing`、`DeleteConfirm`、`Searching` 四种模式。
- 涉及文本编辑、折行或光标定位时，必须按 Unicode 字符和终端显示宽度处理，不能假设一个字符等于一个字节或一个显示列。
- 修改快捷键后，同步事件处理、界面底部提示和 `README.md` 的快捷键表。
- TUI 即使报错也必须恢复 raw mode、备用屏幕、鼠标捕获和光标样式；不要破坏现有恢复流程。

## 测试与交付

- 数据库、筛选、归档、编辑状态或光标算法发生变化时，补充或更新相邻模块的单元测试。
- 至少运行与改动直接相关的测试；通常应运行完整的 `cargo test`，当前测试规模很小。
- CLI 改动应检查 `cargo run -- --list`；注意它会访问默认数据库，因此自动化验证优先通过内存数据库测试完成。
- TUI 渲染或交互改动除了单元测试，还应在真实终端中做最小手动验证，并确认退出后终端状态正常。
- 最终说明应包含：修改内容、运行过的验证、未验证项或已有基线问题。

## 安全与版本控制

- 不执行破坏性 Git 命令，不覆盖或撤销用户已有改动。
- 删除文件前先确认目标；可恢复删除优先于永久删除。
- 按“铁律”提交并推送代码改动，但不要发布 Release、创建标签或修改真实用户数据，除非用户明确要求。
- 如果发现任务外问题，只在交付时单独提醒，不擅自修复。
